1. Verify the fix for `src/pages/AllLeads.tsx` by running the build command or checking for errors.
2. Verify the `createLeadFromSource` mutation is correctly added to `src/convex/webhook.ts`.
3. Check if there are any other missing mutations or issues causing the 500 error.
